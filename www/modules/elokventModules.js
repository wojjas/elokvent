(function () {
  'use strict';

  var footer = angular.module('elokvent.modules', []);

  // Execute bootstrapping code and any dependencies.
  footer.run(['$rootScope',
    function ($rootScope) {

    }]);
})();
